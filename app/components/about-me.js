import Ember from 'ember';

const {
  Component,
} = Ember;

export default Component.extend({
  skills: ['HTML 5', 'CSS3 + SASS', 'JavaScript', 'Ember', 'Responsive Web', 'Git SCM', 'Agile', 'TDD'],
});
